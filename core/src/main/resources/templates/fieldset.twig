<fieldset{{ attributes }}>
    {% if legend %}
        <legend>{{ legend | render }}</legend>
    {% endif %}
    {% for (child in children) %}
        {{ child | render }}
    {% endfor %}
</fieldset>