{{ render(label) }}
{% if content %}
    <label>
        <input type="checkbox" {{ printAttribute("name", name) }} value="{{ value }}"{{ attributes }} {{ checked }} />
        {{ render(content) }}
    </label>
{% else %}
    <input type="checkbox" {{ printAttribute("name", name) }} value="{{ value }}"{{ attributes }} {{ checked }} />
{% endif %}